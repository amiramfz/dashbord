import { useState } from "react";
import body from "./body.module.css";

function Dashbord(props) {
  let s = props.lan;

  return (
    <>
      <div className={body.MainBox}>
        <h2 className={s == 0 ? body.Titlefa : body.Titleen}>
          {s == 0 ? "داشبورد" : "Dashbord"}
        </h2>
        <div className={body.boxes}>
          <h2 className={s == 0 ? body.DashTexfa : body.DashTexen}>
            {s == 0 ? "سلیم" : "salom"}
          </h2>
          <p className={s == 0 ? body.DashTizihFa : body.DashTizihEn}>
            {s == 0 ? "گروهی" : "teampoint"}
          </p>
          <p className={s == 0 ? body.endfa : body.enden}>
            {s == 0 ? "تمام" : "tamam"}
          </p>
        </div>
        <div className={body.boxes}>
          <h2 className={s == 0 ? body.DashTexfa : body.DashTexen}>
            {s == 0 ? "سلیم" : "salom"}
          </h2>
          <p className={s == 0 ? body.DashTizihFa : body.DashTizihEn}>
            {s == 0 ? "گروهی" : "teampoint"}
          </p>
          <p className={s == 0 ? body.endfa : body.enden}>
            {s == 0 ? "تمام" : "tamam"}
          </p>
        </div>
        <div className={body.boxes}>
          <h2 className={s == 0 ? body.DashTexfa : body.DashTexen}>
            {s == 0 ? "سلیم" : "salom"}
          </h2>
          <p className={s == 0 ? body.DashTizihFa : body.DashTizihEn}>
            {s == 0 ? "گروهی" : "teampoint"}
          </p>
          <p className={s == 0 ? body.endfa : body.enden}>
            {s == 0 ? "تمام" : "tamam"}
          </p>
        </div>
        <div className={body.boxes}>
          <h2 className={s == 0 ? body.DashTexfa : body.DashTexen}>
            {s == 0 ? "سلیم" : "salom"}
          </h2>
          <p className={s == 0 ? body.DashTizihFa : body.DashTizihEn}>
            {s == 0 ? "گروهی" : "teampoint"}
          </p>
          <p className={s == 0 ? body.endfa : body.enden}>
            {s == 0 ? "تمام" : "tamam"}
          </p>
        </div>
        <div className={body.boxes}>
          <h2 className={s == 0 ? body.DashTexfa : body.DashTexen}>
            {s == 0 ? "سلیم" : "salom"}
          </h2>
          <p className={s == 0 ? body.DashTizihFa : body.DashTizihEn}>
            {s == 0 ? "گروهی" : "teampoint"}
          </p>
          <p className={s == 0 ? body.endfa : body.enden}>
            {s == 0 ? "تمام" : "tamam"}
          </p>
        </div>
        <div className={body.boxes}>
          <h2 className={s == 0 ? body.DashTexfa : body.DashTexen}>
            {s == 0 ? "سلیم" : "salom"}
          </h2>
          <p className={s == 0 ? body.DashTizihFa : body.DashTizihEn}>
            {s == 0 ? "گروهی" : "teampoint"}
          </p>
          <p className={s == 0 ? body.endfa : body.enden}>
            {s == 0 ? "تمام" : "tamam"}
          </p>
        </div>
        <div className={body.boxes}>
          <h2 className={s == 0 ? body.DashTexfa : body.DashTexen}>
            {s == 0 ? "سلیم" : "salom"}
          </h2>
          <p className={s == 0 ? body.DashTizihFa : body.DashTizihEn}>
            {s == 0 ? "گروهی" : "teampoint"}
          </p>
          <p className={s == 0 ? body.endfa : body.enden}>
            {s == 0 ? "تمام" : "tamam"}
          </p>
        </div>
        <div className={body.boxes}>
          <h2 className={s == 0 ? body.DashTexfa : body.DashTexen}>
            {s == 0 ? "سلیم" : "salom"}
          </h2>
          <p className={s == 0 ? body.DashTizihFa : body.DashTizihEn}>
            {s == 0 ? "گروهی" : "teampoint"}
          </p>
          <p className={s == 0 ? body.endfa : body.enden}>
            {s == 0 ? "تمام" : "tamam"}
          </p>
        </div>
        <div className={body.boxes}>
          <h2 className={s == 0 ? body.DashTexfa : body.DashTexen}>
            {s == 0 ? "سلیم" : "salom"}
          </h2>
          <p className={s == 0 ? body.DashTizihFa : body.DashTizihEn}>
            {s == 0 ? "گروهی" : "teampoint"}
          </p>
          <p className={s == 0 ? body.endfa : body.enden}>
            {s == 0 ? "تمام" : "tamam"}
          </p>
        </div>
        <div className={body.boxes}>
          <h2 className={s == 0 ? body.DashTexfa : body.DashTexen}>
            {s == 0 ? "سلیم" : "salom"}
          </h2>
          <p className={s == 0 ? body.DashTizihFa : body.DashTizihEn}>
            {s == 0 ? "گروهی" : "teampoint"}
          </p>
          <p className={s == 0 ? body.endfa : body.enden}>
            {s == 0 ? "تمام" : "tamam"}
          </p>
        </div>
        <div className={body.boxes}>
          <h2 className={s == 0 ? body.DashTexfa : body.DashTexen}>
            {s == 0 ? "سلیم" : "salom"}
          </h2>
          <p className={s == 0 ? body.DashTizihFa : body.DashTizihEn}>
            {s == 0 ? "گروهی" : "teampoint"}
          </p>
          <p className={s == 0 ? body.endfa : body.enden}>
            {s == 0 ? "تمام" : "tamam"}
          </p>
        </div>
        <div className={body.boxes}>
          <h2 className={s == 0 ? body.DashTexfa : body.DashTexen}>
            {s == 0 ? "سلیم" : "salom"}
          </h2>
          <p className={s == 0 ? body.DashTizihFa : body.DashTizihEn}>
            {s == 0 ? "گروهی" : "teampoint"}
          </p>
          <p className={s == 0 ? body.endfa : body.enden}>
            {s == 0 ? "تمام" : "tamam"}
          </p>
        </div>
        <div className={body.boxes}>
          <h2 className={s == 0 ? body.DashTexfa : body.DashTexen}>
            {s == 0 ? "سلیم" : "salom"}
          </h2>
          <p className={s == 0 ? body.DashTizihFa : body.DashTizihEn}>
            {s == 0 ? "گروهی" : "teampoint"}
          </p>
          <p className={s == 0 ? body.endfa : body.enden}>
            {s == 0 ? "تمام" : "tamam"}
          </p>
        </div>
        <div className={body.boxes}>
          <h2 className={s == 0 ? body.DashTexfa : body.DashTexen}>
            {s == 0 ? "سلیم" : "salom"}
          </h2>
          <p className={s == 0 ? body.DashTizihFa : body.DashTizihEn}>
            {s == 0 ? "گروهی" : "teampoint"}
          </p>
          <p className={s == 0 ? body.endfa : body.enden}>
            {s == 0 ? "تمام" : "tamam"}
          </p>
        </div>
        <div className={body.boxes}>
          <h2 className={s == 0 ? body.DashTexfa : body.DashTexen}>
            {s == 0 ? "سلیم" : "salom"}
          </h2>
          <p className={s == 0 ? body.DashTizihFa : body.DashTizihEn}>
            {s == 0 ? "گروهی" : "teampoint"}
          </p>
          <p className={s == 0 ? body.endfa : body.enden}>
            {s == 0 ? "تمام" : "tamam"}
          </p>
        </div>
        <div className={body.boxes}>
          <h2 className={s == 0 ? body.DashTexfa : body.DashTexen}>
            {s == 0 ? "سلیم" : "salom"}
          </h2>
          <p className={s == 0 ? body.DashTizihFa : body.DashTizihEn}>
            {s == 0 ? "گروهی" : "teampoint"}
          </p>
          <p className={s == 0 ? body.endfa : body.enden}>
            {s == 0 ? "تمام" : "tamam"}
          </p>
        </div>
      </div>
    </>
  );
}

export default Dashbord;
